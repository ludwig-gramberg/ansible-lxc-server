---

- hosts: all
  vars:
    ansible_user: "{{ config_user_name }}"
    ansible_become_pass: "{{ config_user_password }}"
  become: true
  become_user: root
  become_method: sudo
  tasks:
    - name: upgrade packages
      apt:
        upgrade: dist
        update_cache: yes